<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Sidewalks" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200 fixed w-full top-0 z-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- Logo and primary navigation -->
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center space-x-2 text-xl font-bold text-gray-900 hover:text-blue-600 transition duration-200" do %>
              <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"></path>
              </svg>
              <span>Sidewalks</span>
            <% end %>

            <% if user_signed_in? %>
              <!-- Main navigation links -->
              <div class="hidden md:flex items-center space-x-8 ml-10">
                <%= link_to "Activities", activities_path,
                    class: "text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-200 #{'text-blue-600 border-b-2 border-blue-600' if current_page?(activities_path) || params[:controller] == 'activities'}" %>
                <%= link_to "Playlists", playlists_path,
                    class: "text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-200 #{'text-blue-600 border-b-2 border-blue-600' if current_page?(playlists_path) || params[:controller] == 'playlists'}" %>
                <%= link_to "Calendar", "#",
                    class: "text-gray-700 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-200" %>
              </div>
            <% end %>
          </div>

          <!-- User menu -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <!-- User dropdown -->
              <div class="relative" data-controller="dropdown">
                <button data-action="click->dropdown#toggle" class="flex items-center space-x-2 text-gray-700 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 rounded-lg px-3 py-2">
                  <div class="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-sm font-medium">
                    <%= current_user.name.first.upcase %>
                  </div>
                  <span class="hidden md:block text-sm font-medium"><%= current_user.name %></span>
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
                  </svg>
                </button>

                <!-- Dropdown menu -->
                <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-lg shadow-lg border border-gray-200 py-1 z-50">
                  <%= link_to "Profile", edit_user_registration_path,
                      class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200" %>
                  <%= link_to "Google Calendar", "#",
                      class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200" %>
                  <div class="border-t border-gray-100 my-1"></div>
                  <%= link_to "Sign Out", destroy_user_session_path, method: :delete,
                      class: "block px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 transition duration-200" %>
                </div>
              </div>
            <% else %>
              <!-- Guest user buttons -->
              <div class="flex items-center space-x-3">
                <%= link_to "Sign In", new_user_session_path,
                    class: "text-gray-700 hover:text-gray-900 px-3 py-2 text-sm font-medium transition duration-200" %>
                <%= link_to "Sign Up", new_user_registration_path,
                    class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-sm font-medium transition duration-200" %>
              </div>
            <% end %>

            <!-- Mobile menu button -->
            <div class="md:hidden">
              <button data-controller="mobile-menu" data-action="click->mobile-menu#toggle" class="text-gray-700 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 rounded-lg p-2">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
              </button>
            </div>
          </div>
        </div>

        <!-- Mobile menu -->
        <% if user_signed_in? %>
          <div data-mobile-menu-target="menu" class="hidden md:hidden border-t border-gray-200 py-4">
            <div class="space-y-1">
              <%= link_to "Activities", activities_path,
                  class: "block px-3 py-2 text-gray-700 hover:text-blue-600 hover:bg-gray-50 rounded-lg text-sm font-medium transition duration-200" %>
              <%= link_to "Playlists", playlists_path,
                  class: "block px-3 py-2 text-gray-700 hover:text-blue-600 hover:bg-gray-50 rounded-lg text-sm font-medium transition duration-200" %>
              <%= link_to "Calendar", "#",
                  class: "block px-3 py-2 text-gray-700 hover:text-blue-600 hover:bg-gray-50 rounded-lg text-sm font-medium transition duration-200" %>
              <div class="border-t border-gray-200 my-2"></div>
              <%= link_to "Profile", edit_user_registration_path,
                  class: "block px-3 py-2 text-gray-700 hover:text-blue-600 hover:bg-gray-50 rounded-lg text-sm font-medium transition duration-200" %>
              <%= link_to "Sign Out", destroy_user_session_path, method: :delete,
                  class: "block px-3 py-2 text-gray-700 hover:text-blue-600 hover:bg-gray-50 rounded-lg text-sm font-medium transition duration-200" %>
            </div>
          </div>
        <% end %>
      </div>
    </nav>

    <!-- Flash messages -->
    <% if notice %>
      <div class="fixed top-20 left-1/2 transform -translate-x-1/2 z-40 bg-green-100 border border-green-400 text-green-700 px-6 py-3 rounded-lg shadow-lg" data-controller="flash" data-action="click->flash#dismiss">
        <div class="flex items-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
          <%= notice %>
          <button data-action="click->flash#dismiss" class="ml-4 text-green-500 hover:text-green-700">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="fixed top-20 left-1/2 transform -translate-x-1/2 z-40 bg-red-100 border border-red-400 text-red-700 px-6 py-3 rounded-lg shadow-lg" data-controller="flash" data-action="click->flash#dismiss">
        <div class="flex items-center">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L4.082 15.5c-.77.833.192 2.5 1.732 2.5z"></path>
          </svg>
          <%= alert %>
          <button data-action="click->flash#dismiss" class="ml-4 text-red-500 hover:text-red-700">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </button>
        </div>
      </div>
    <% end %>

    <!-- Main content -->
    <main class="pt-16 min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
